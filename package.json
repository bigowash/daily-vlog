{
  "name": "vlog-logbook",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "NODE_OPTIONS=--enable-source-maps npx astro dev",
    "build": "NODE_OPTIONS=--enable-source-maps npx astro build",
    "preview": "npx astro preview",
    "astro": "npx astro",
    "clean-install": "rm -rf node_modules package-lock.json && npm install"
  },
  "dependencies": {
    "astro": "^5.7.4"
  }
}
